feature: **Correlated WebView navigation lifecycle**: the runtime now journals and emits child main-frame `started`, `redirected`, `finished`, `failed`, and `cancelled` records with lossless opaque ids, bounded failure classes, and a compatibility `webview:navigate` alias; the macOS, WebView2, and WebKitGTK system-WebView hosts supply engine adapters. One dedicated dynamic-origin executable matrix covers correlation, supersession, teardown, capacity reuse, encoded asset provenance, network failure, and real self-signed TLS failure. The pre-TLS matrix is process-locally proven on macOS; the exact TLS-enhanced candidate remains wired but unobserved on isolated macOS, hosted Linux/WebKitGTK, and hosted Windows/WebView2, and cross-builds do not substitute for those runs. CEF remains deliberately unsupported until a separate design-recorded spike proves document-terminal identity and complete `OnBeforeClose`-before-`CefShutdown` teardown.
