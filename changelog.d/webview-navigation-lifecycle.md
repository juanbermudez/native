feature: **Correlated WebView navigation lifecycle**: the runtime now journals and emits child main-frame `started`, `redirected`, `finished`, `failed`, and `cancelled` records with lossless opaque ids, bounded failure classes, and a compatibility `webview:navigate` alias; the macOS, WebView2, and WebKitGTK system-WebView hosts supply engine adapters, and one cross-platform executable matrix covers correlation, supersession, teardown, capacity reuse, and encoded asset provenance. CEF remains deliberately unsupported until a separate design-recorded spike proves document-terminal identity and complete `OnBeforeClose`-before-`CefShutdown` teardown.
