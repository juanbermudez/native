feature: **macOS HUD windows**: model-declared canvas windows can request a transparent, chromeless, non-activating top-attached presentation; one live window can retarget compact/list/detail geometry in place with an interruptible, pixel-quantized, finite-lifetime spring, snaps the same transition under Reduce Motion, uses the system HUD material as a backdrop sibling beneath the full-bleed canvas, and reanchors flush with the top-center display edge when its display changes. Keeping application views outside the visual-effect subtree preserves authored canvas colors instead of washing the UI through vibrancy. The chassis keeps square top corners and continuous rounded bottom corners, preventing a second material color from bleeding through the window corners. The first click both activates its control and establishes panel-local keyboard routing without foreground-app activation. Startup first-frame draining reaches secondary canvases declared by the main canvas's installing frame before the AppKit run loop starts, keeping the HUD inside the existing GPU first-frame budget. `NATIVE_SDK_FORCE_REDUCE_MOTION=1` enables deterministic live/automation proof without changing the user's system setting.
